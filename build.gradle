apply plugin: 'groovy'
apply plugin: 'maven'


dependencies {
    compile "com.android.tools.build:gradle:3.1.3"
  //gradle sdk
    compile gradleApi()
  //groovy sdk
    compile localGroovy()
    compile "com.android.tools.build:gradle-api:2.3.0"
    compile "javassist:javassist:3.12.1.GA"
    compile "commons-io:commons-io:2.5"
}


repositories {
  mavenCentral()
    maven {
        url 'https://maven.google.com/'
        name 'Google'
    }
}
//以上都为固定写法

//group和version
group='com.test'
version='1.0.0'

//打包到本地或者远程Maven库
uploadArchives {
  repositories {
      mavenDeployer {
          //提交到远程服务器：
          // repository(url: "http://www.xxx.com/repos") {
          //    authentication(userName: "admin", password: "admin")
          // }
          //本地的Maven地址设置为E:/Maven
          repository(url: uri('/Users/huaixiang/Projects/github/JavassistDemo/maven'))
      }
  }
}